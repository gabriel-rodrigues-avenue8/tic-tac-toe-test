import {BrowserRouter} from "react-router-dom";
import {Route} from "react-router";

export const TicTacToeModule = (): JSX.Element => {
  return (<BrowserRouter>
    <Switch>
      <Route>

      </Route>
    </Switch>
  </BrowserRouter>)
}
